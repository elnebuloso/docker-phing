<?xml version="1.0" encoding="UTF-8"?>


<project default="init:laravel-lumen5">


    <property name="init.laravel-lumen5.description" value="init laravel-lumen5 application" override="true"/>


    <target name="init:laravel-lumen5:help">
        <echo message="${init.laravel-lumen5.description}"/>
    </target>


    <target name="init:laravel-lumen5:before">
        <echo message="not implemented"/>
    </target>


    <target name="init:laravel-lumen5" depends="init:laravel-lumen5:before">
        <fail message="${projectDirMain} exists for creating laravel-lumen5 application">
            <condition>
                <available file="${projectDirMain}" type="dir"/>
            </condition>
        </fail>

        <exec command="composer create-project --prefer-dist --no-dev --remove-vcs laravel/lumen ${projectDirMain} ^5" passthru="true" checkreturn="true" level="info"/>
        <delete dir="${projectDirMain}/.git" quiet="true"/>

        <copy todir="${projectRoot}" overwrite="true">
            <fileset dir="${phingCommonsRoot}/resources/init/laravel-lumen5/0">
                <include name="**"/>
            </fileset>
        </copy>
    </target>


</project>
