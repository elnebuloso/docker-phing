<?xml version="1.0" encoding="UTF-8"?>


<project default="docker:push">


    <property name="docker.push.description" value="push project image to a registry" override="true"/>


    <target name="docker:push:help">
        <echo message="${docker.push.description}"/>
    </target>


    <target name="docker:push">
        <fail unless="env.REGISTRY_HOST" message="missing environment variable REGISTRY_HOST"/>

        <if>
            <isset property="env.PHING_VERSION_SUFFIX"/>
            <then>
                <property name="project.env.version" value="${projectVersion}-${env.PHING_VERSION_SUFFIX}" override="true"/>
            </then>
            <else>
                <property name="project.env.version" value="${projectVersion}" override="true"/>
            </else>
        </if>

        <exec command="docker tag ${projectName} ${env.REGISTRY_HOST}/${projectName}" passthru="true" checkreturn="true" level="info" dir="${projectRoot}"/>
        <exec command="docker tag ${projectName} ${env.REGISTRY_HOST}/${projectName}:${project.env.version}" passthru="true" checkreturn="true" level="info" dir="${projectRoot}"/>
        <exec command="docker push ${env.REGISTRY_HOST}/${projectName}" passthru="true" checkreturn="true" level="info" dir="${projectRoot}"/>
        <exec command="docker push ${env.REGISTRY_HOST}/${projectName}:${project.env.version}" passthru="true" checkreturn="true" level="info" dir="${projectRoot}"/>
    </target>


</project>